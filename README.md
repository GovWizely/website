# SETUP

## Dependencies

- Ruby 2.*

- Bundler

  Run the following to install one
  ```unix
  $ gem install bundler
  ```

## Setup

- Install gems 
  ```unix
  $ bundle install --path .bundle
  ```

- Start jekyll server
  ```unix
  $ bundle exec jekyll s
  ```
